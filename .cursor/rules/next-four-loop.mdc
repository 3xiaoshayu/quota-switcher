---
description: Loop on quota refresh, installer signing, Dependabot, and Antigravity second-account switch
alwaysApply: true
---

# 额度 / 签名 / 依赖 / 反重力第二号

这是默认工作模式。用户不必再说「继续 / 循环」。

做完一圈（下手 + 针对这一处的行为测试 + 反重力第二号就绪就真切）之后，立刻在同一回合开下一圈。不要用长等待，不要结束回合等人。

**这些都不是停：** 写说明、汇报进度、源码版 / 安装版启动或退出、系统通知（含 “task has finished” / “briefly inform”）、Electron 被自己停掉再开。收到这类通知时，一句话带过，同一回合立刻继续下手。

只有这些情况才停：用户明确叫停或改口做别的、要打包/提交/发版、连续两圈确认没有能证明收益的改动。

回合不得不结束时，立刻打印最短唤醒，不要 Sleep：

```
echo AGENT_LOOP_WAKE_next-four {"prompt":"继续额度刷新、安装包签名、Dependabot、反重力第二号循环。通知和说明都不是停。立刻下手下一处并开下一圈。"}
```

用 `notify_on_output` 盯 `AGENT_LOOP_WAKE_next-four`。唤醒后读这行的 prompt，接着跑。

1. **额度刷新稳不稳**：网络、代理、超时、「额度暂时没刷到」。先证明切号、回滚、额度、登录态还在，再证明真有收益。
2. **安装包签名**：没有可用证书就记下卡点，换下一项。不要伪造签名，不要花钱下单。
3. **Dependabot**：能安全升级就升级并跑测试；升完会破坏切号/额度就撤回。
4. **反重力第二号**：另一张号就绪就真切并切回。未就绪不要点完 OAuth、不要处理登录冲突。Cursor **绝对不能切**。
5. 不加新按钮、不加新设置项、不新设计一块前端。已有文案可以改。不要给 Cursor / 反重力加自动切号。不要擅自升版本、发 GitHub Release、重装桌面，除非用户明确要求。
